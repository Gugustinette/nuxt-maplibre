{
  "private": true,
  "name": "nuxt-maplibre-playground",
  "type": "module",
  "scripts": {
    "dev": "nuxi dev",
    "dev:ssg": "npm run generate && npx serve .output/public",
    "build": "nuxi build",
    "generate": "nuxi generate"
  },
  "devDependencies": {
    "nuxt": "latest"
  }
}
